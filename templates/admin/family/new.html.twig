{% extends 'layouts/app.html.twig' %}

{% block title %}{{ 'family.new.title'|trans({}, 'admin') }} · {{ app_name }}{% endblock %}

{% block stylesheets %}{{ parent() }}
<style>
.ts-wrapper { font-size: 0.875rem; }
.ts-control { border-radius: 0.75rem !important; border-color: #e5e7eb !important; background-color: #f9fafb !important; padding: 0.5rem 0.875rem !important; box-shadow: none !important; gap: 0.25rem; }
.ts-control:focus-within { border-color: #876baf !important; background-color: #fff !important; box-shadow: 0 0 0 3px rgb(135 107 175 / 0.2) !important; }
.ts-control .item { background: #ece6f4; color: #574a68; border: 1px solid #d8cce8; border-radius: 0.5rem; padding: 0.125rem 0.375rem; font-size: 0.8125rem; }
.ts-control .item .remove { color: #876baf; border-left-color: #d8cce8; }
.ts-dropdown { border-radius: 0.75rem; border-color: #e5e7eb; box-shadow: 0 4px 12px rgb(0 0 0 / 0.08); overflow: hidden; }
.ts-dropdown .option { padding: 0.5rem 0.875rem; }
.ts-dropdown .option.active { background-color: #f6f3f9; color: #1f192a; }
</style>
{% endblock %}

{% block content %}
<div class="px-6 py-8 max-w-2xl mx-auto space-y-8">

    <div>
        <div class="flex items-center gap-2 text-sm text-gray-500 mb-1">
            <a href="{{ path('app_admin') }}" class="hover:text-plum-600 transition-colors">{{ 'dashboard.title'|trans({}, 'admin') }}</a>
            {{ ux_icon('heroicons:chevron-right', {class: 'h-4 w-4'}) }}
            <a href="{{ path('app_admin_centres_index') }}" class="hover:text-plum-600 transition-colors">{{ 'centres.title'|trans({}, 'admin') }}</a>
            {{ ux_icon('heroicons:chevron-right', {class: 'h-4 w-4'}) }}
            <a href="{{ path('app_admin_centres_edit', {id: centre.id}) }}" class="hover:text-plum-600 transition-colors">{{ centre.name }}</a>
            {{ ux_icon('heroicons:chevron-right', {class: 'h-4 w-4'}) }}
            <a href="{{ path('app_admin_families_index', {centreId: centre.id}) }}" class="hover:text-plum-600 transition-colors">{{ 'families.title'|trans({}, 'admin') }}</a>
            {{ ux_icon('heroicons:chevron-right', {class: 'h-4 w-4'}) }}
        </div>
        <h1 class="text-2xl font-bold text-gray-900">{{ 'family.new.title'|trans({}, 'admin') }}</h1>
    </div>

    <div class="rounded-2xl border border-gray-200 bg-white shadow-xs overflow-hidden">
        <div class="px-6 py-4 border-b border-gray-100">
            <h2 class="text-base font-semibold text-gray-900">{{ 'family.new.title'|trans({}, 'admin') }}</h2>
        </div>
        <form method="post" class="px-6 py-6 space-y-5">
            <input type="hidden" name="_token" value="{{ csrf_token('new_family') }}">

            <twig:Form:Field
                id="name" name="name" type="text"
                label="{{ 'family.field.name'|trans({}, 'admin') }}"
                value="{{ values.name }}"
                :error="errors.name ?? null"
                required="true" />

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-0.5">
                    {{ 'family.field.head'|trans({}, 'admin') }}
                </label>
                <p class="text-xs text-gray-400 mb-2">{{ 'family.field.head_help'|trans({}, 'admin') }}</p>
                <select name="head"
                        data-controller="symfony--ux-autocomplete--autocomplete"
                        data-symfony--ux-autocomplete--autocomplete-url-value="{{ path('ux_entity_autocomplete', {alias: 'teacher_admin'}) }}"
                        data-symfony--ux-autocomplete--autocomplete-min-characters-value="2"
                        data-symfony--ux-autocomplete--autocomplete-no-results-found-text-value="{{ 'autocomplete.no_results'|trans({}, 'admin') }}"
                        class="w-full">
                    {% if selectedHead %}
                        <option value="{{ selectedHead.id }}" selected>{{ selectedHead.name.lastName }}, {{ selectedHead.name.firstName }}</option>
                    {% endif %}
                </select>
            </div>

            <div class="flex items-center justify-end gap-3 pt-2">
                <a href="{{ path('app_admin_families_index', {centreId: centre.id}) }}"
                   class="rounded-xl border border-gray-200 px-4 py-2.5 text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">
                    {{ 'family.cancel'|trans({}, 'admin') }}
                </a>
                <button type="submit"
                        class="rounded-xl bg-plum-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-plum-700 transition-colors shadow-xs">
                    {{ 'family.save'|trans({}, 'admin') }}
                </button>
            </div>
        </form>
    </div>

</div>
{% endblock %}
