<div {{ attributes }}>
    <div class="rounded-2xl border border-gray-200 bg-white shadow-xs overflow-hidden">
        <div class="px-6 py-4 border-b border-gray-100">
            <h2 class="text-base font-semibold text-gray-900">{{ 'workcenter.edit.title'|trans({}, 'companies') }}</h2>
        </div>
        <form
            method="post"
            class="px-6 py-6 space-y-5"
            data-action="submit->live#action:prevent"
            data-live-action-param="save"
        >
            <div class="grid grid-cols-1 gap-5 sm:grid-cols-3">

                <div class="sm:col-span-2">
                    <twig:Form:Field
                        id="name"
                        name="name"
                        label="{{ 'workcenters.field.name'|trans({}, 'companies') }}"
                        value="{{ name }}"
                        :error="errors.name ?? null"
                        data-model="on(change)|name"
                    />
                </div>

                <div>
                    <twig:Form:Field
                        id="city"
                        name="city"
                        label="{{ 'workcenters.field.city'|trans({}, 'companies') }}"
                        value="{{ city }}"
                        :error="errors.city ?? null"
                        data-model="on(change)|city"
                    />
                </div>

            </div>

            <div class="flex items-center justify-end gap-3 pt-2">
                <a href="{{ path('app_companies_edit', {id: workcenter.company.id}) }}"
                   class="rounded-xl border border-gray-200 px-4 py-2.5 text-sm font-medium text-gray-700
                          hover:bg-gray-50 transition-colors">
                    {{ 'workcenter.edit.cancel'|trans({}, 'companies') }}
                </a>
                <button type="submit"
                        class="rounded-xl bg-plum-600 px-4 py-2.5 text-sm font-semibold text-white
                               hover:bg-plum-700 transition-colors shadow-xs">
                    {{ 'workcenter.edit.save'|trans({}, 'companies') }}
                </button>
            </div>
        </form>
    </div>
</div>
