# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
DATABASE_URL="sqlite:///:memory:"
MIGRATIONS_PATH=migrations/sqlite
# Valor solo para tests: el hub Mercure no se levanta (CollectingHub en memoria),
# pero el bundle exige que el secreto esté definido al construir el contenedor.
MERCURE_JWT_SECRET='!TestOnlyMercureJWTSecretChangeMe!'
APP_LOG=false
